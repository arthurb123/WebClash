import WebRTCConnection from './webrtcConnection';
import { ServerOptions } from '@geckos.io/common/lib/typings';
export default class ConnectionsManagerServer {
    options: ServerOptions;
    connections: Map<any, any>;
    constructor(options: ServerOptions);
    private makeRandomId;
    private createId;
    getConnection: (id: string | undefined) => any;
    getConnections: () => void;
    createConnection(): Promise<WebRTCConnection>;
    deleteConnection(connection: WebRTCConnection): void;
}
//# sourceMappingURL=connectionsManager.d.ts.map